query publications($request: PublicationsRequest!) {
  result: publications(request: $request) {
    items {
      ... on Post {
        ...PostFields
      }
      ... on Mirror {
        ...MirrorFields
      }
      ... on Comment {
        ...CommentFields
      }
      ... on Quote {
        ...QuoteFields
      }
    }
    pageInfo {
      prev
      next
    }
  }
}
